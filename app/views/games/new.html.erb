<h1>New game</h1>

<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
      <% @game.errors.full_messages.each do |msg| %>
      <% end %>
  <% end %>

  <div id='game-bar'>
    <% [ 21, 0 ].each do |score| %>
      <div class='side'>
        <div class='player-box'>
          sdlkfj
        </div>
        <div class='score-box'>
          <%= score %>
        </div>
      </div>
    <% end %>
  </div>

  <!--
  <%= f.select :winner_id, @users.collect { |u| [u.email, u.id] }, {:include_blank => true} %>
  <%= f.select :winner_double_id, @users.collect { |u| [u.email, u.id] }, {:include_blank => true} %>

  <%= f.select :loser_id, @users.collect { |u| [u.email, u.id] }, {:include_blank => true} %>
  <%= f.select :loser_double_id, @users.collect { |u| [u.email, u.id] }, {:include_blank => true} %>
  -->
<% end %>

<%= link_to 'Back', games_path %>
